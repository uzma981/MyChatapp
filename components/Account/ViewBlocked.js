import { View, Text } from "react-native";
import React from "react";
import { TouchableOpacity } from "react-native-web";

export default function ViewBlocked() {
  return (
    <View>
      <Text>Blocked Contacts go here</Text>
    </View>
  );
}
